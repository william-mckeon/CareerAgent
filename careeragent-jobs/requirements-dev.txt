# =============================================================================
# requirements-dev.txt  — test-only dependencies for careeragent-jobs.
#
#   pip install -r requirements.txt -r requirements-dev.txt
#   python -m pytest tests/ -q
#
# httpx (needed by starlette's TestClient) is already in requirements.txt.
# =============================================================================

pytest>=8.0,<9.0
pytest-asyncio>=0.23,<1.0   # async test support (worker/jobtypes/verify are coroutines)
