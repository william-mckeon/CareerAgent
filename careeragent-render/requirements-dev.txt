-r requirements.txt
pytest>=8.0.0
pytest-asyncio>=0.23.0

# TEST-ONLY: FastAPI's TestClient (Starlette) needs an HTTP client. This is NOT a
# runtime dependency — the service makes no HTTP calls — so it stays out of
# requirements.txt and the container image.
httpx>=0.27.0
